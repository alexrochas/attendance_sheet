<h1>Users</h1>
<% @users = User.all %>
<table>
  <thead>
    <tr>
      <td>Email</td>
      <td>Admin</td>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <% if user.admin %>
            <td>✓</td>
        <% else %>
            <td></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
